# Figma to app redesign implementation

User scope: full approved mobile and desktop MOTOCAST redesign, added explicitly on 2026-09-17 to the active 0.3.0 implementation and authorized PR / merge / deployment. Preserve existing provider, ownership, immutable sharing, schedule and budget contracts. Shared-course copy and approved old-history retirement remain in scope.

## Source and assets

Figma file: https://www.figma.com/design/wVNriNWb1OlF21DVq8rqlJ
Core desktop nodes: 31:778 home, 31:779 editor, 31:780 summary, 31:781 collection list. Mobile: 20:311 home, 2:161 editor, 2:162 picker, 12:137 schedule, 12:138 summary, 28:760 collections. Responsive rules: 37:2219. Current reference contexts were read through Figma MCP, not inferred from screenshots alone. Raw generated exports are temporary references and must not be committed.

`public/figma/motorcycle.svg`, `sunny.svg`, `partly-cloudy.svg` are exact Figma SVG_STRING exports with outlined text from nodes 33:883, 35:887 and 61:3018 (Noto Emoji), not hand-authored vector substitutes. Maps are dynamic Kakao data in the application; the prototype's decorative geometry and example journeys are not production data.

## READY interface contract

- Explicit home / editor / summary / collections views with browser navigation and existing public-share entry preserved. Reuse provider and request-gating orchestration; split presentation and dialogs by responsibility.
- Home: separate new-route and saved-route actions, motorcycle to the right with breathing room; recent owned courses and real empty state.
- Editor: origin, stable ordered waypoint occurrences, destination, reset beneath destination, schedule entry, common favorites. Arrow reordering recomputes visible labels without wrapping. Preserve current stop kinds and safety limits.
- Search: full-screen mobile, centered desktop dialog; verified provider selections, stale-response cancellation, name/address hierarchy, zero-result/error/retry states.
- Schedule: calendar and separate hour/minute choices with confirm/cancel, past-date/time rejection, fresh schedule for saved courses. Date/time changes preserve an explicitly pending collection-share intention while invalidating old approvals.
- Summary: map and readable time/weather cards, actual total/ride/rest/arrival values, weather emoji and text, edit and share/save actions. Unknown/stale weather remains explicit.
- Sharing retains full server-built preview and separate explicit publish. Saving persists the complete course without date/time and refreshes home/list. Existing version/delete/revoke management remains reachable.
- The received-summary new-schedule action retrieves a minimal reusable course through authenticated `get_shared_course` / same-origin POST, then opens the editor with a blank schedule. It does not automatically save, calculate or publish. Anonymous snapshot resolution never includes verification proofs. Private course and bearer remain only in component memory, with hash-change/unmount response isolation.
- Favorites are one per-user set of at most three, used in all three place roles, not a global cross-account list. Persistence uses a narrow owner-only database boundary; route and collection use still revalidates signed provider proof.
- Responsive: <=767px 20px gutters / full-screen search / bottom dialogs / one-column cards; 768-1199px 32px gutters / 360px editor / two-column saved cards; >=1200px max1280px content / 456px editor / three-column saved cards. The approved intermediate-width note permits vertically arranged results when space is insufficient: map/weather summary stacks below 1024px while editor and collection breakpoints remain unchanged. Keep 44px minimum controls, keyboard focus/escape/return and bounded modal scrolling.

## Verification plan

Normal and error navigation, no-result search, favorite duplicate/limit/account separation, repeated-place waypoint ordering and labels, date/time cancellation and fresh schedule, shared-copy ownership, share explicit approval, save/retry/late-response isolation. Browser widths include 320, 390, 768, 1024 and 1440. Verify horizontal clipping/overlap, modal/body scroll, focus and history navigation. Run full required static/unit/build/browser baseline and database role/concurrency suites, then exact candidate CI-only, Preview and Production gates from verification-and-review.md. No hosted mutation has occurred at this checkpoint.

## Intermediate evidence before UI replacement

Writer shared-feature candidate: Vitest 68 files / 575 PASS; WSL Node 20.20.0 keyless Playwright 32 PASS / 2 connected-only SKIP; typecheck, build 16 routes, Deno five entrypoints, operator unit 7 PASS. Four update-page expectations initially failed because their prior-release counts/text were fixed; expectations were updated to the actual new release and the focused four plus complete local browser suite passed. These results precede the full UI replacement and are not final UI verification.

Fresh database correction: the first fresh-chain harness copied Auth prerequisites but omitted the stock Supabase public default grants, causing authenticated table SELECT to fail. A new isolated `motocast_release_chain_20260917` database was initialized from the official image's initial public grants, then the existing twelve migrations were applied as `postgres`. Auth/RLS/budget 35 and live ACL 137 PASS; authenticated trip SELECT true and service-role trip INSERT false. The final two new migrations will be checked on this correct baseline; no post-migration permission grant was used to manufacture a pass.

## Lead review queue during implementation

These observations concern intermediate files, not a fixed release candidate. Writer correction and affected tests remain required before closure.

| ID | Severity | Observation / required behavior | State |
| --- | --- | --- | --- |
| UI-REV-1 | HIGH | Do not nest the picker search form inside the planner form; searching must not submit a route calculation or cause hydration errors. | Assigned |
| UI-REV-2 | MEDIUM | Favorites read and mutation lifecycles must prevent late lists from erasing a successful add, preserve operation failures after refresh, clear busy reliably, and isolate account changes. Same-user token refresh is not an account switch. | Assigned |
| UI-REV-3 | HIGH | `jsonb_object_length(jsonb)` is absent in target PostgreSQL 17; replace with a safe object-key check and exercise real add RPCs. | Assigned |
| UI-REV-4 | MEDIUM | Initial home copy and native date/select placeholders do not satisfy the approved Figma home wording/cards, calendar and hour/minute pickers. Implement the specified UI and favorite management/register states. | Assigned |
| UI-REV-5 | HIGH | Embedded private planning from a public share must not emit app navigation hashes that the parent's bearer resolver interprets as a new share. Use memory navigation and test real new-share hash replacement separately. | Design safeguard assigned |
| UI-REV-6 | MEDIUM | Calendar month state must not derive a Seoul date through local-time getters that can open the preceding month in a western time zone. New saved-course schedules require an explicitly selected time, and confirmation must reject a departure that has become past while the dialog is open. | Assigned |
| UI-REV-7 | MEDIUM | Waypoint settings errors must appear and receive focus inside the active modal. Actual role changes reset the role-specific dwell default, unchanged roles retain custom dwell, and deletion must focus an existing adjacent control rather than the removed `.waypoint-remove` selector. The displayed pass-through choice is `통과`. | Assigned |
| UI-REV-8 | HIGH | At 390x844, closing the schedule modal left `#editor` entirely blank because legacy mobile-overlay CSS kept `.planner-panel` at `visibility:hidden`; a fresh new-route action did not recover it. The separate editor view must render in normal flow independently of the old `plannerOpen` overlay, including 768–820px where the old compact breakpoint differs from the new contract. | Assigned |
| UI-REV-9 | MEDIUM | Explicit navigation retained the preceding long editor's `scrollY`, leaving the home heading hidden behind the header. New view navigation must establish the view's starting scroll and keyboard focus; browser back/forward must remain usable. | Assigned |
| UI-REV-10 | HIGH | The old fixed-minimum timeline columns overflow the new narrow desktop summary column. The approved result heading, total/ride/rest/arrival strip, shared own/received summary presentation, emoji cards and share/save choice flow are not yet fully implemented. Complete the original result-screen contract before final baseline; remove the nonfunctional current-location button without introducing a new location permission. | Assigned |
| UI-REV-11 | MEDIUM | The new save-only collection modal clears its form after success while list refresh overwrites the success message, and the saved list is absent in this mode. Preserve an explicit named saved result with close/list actions, and guard pending save/close/rename with one synchronous operation boundary. | Assigned |

## Intermediate visual review

An owner-private, keyless source snapshot was run with Node 20.20.0 and Next.js development mode on loopback port 4307. No `.env` file or hosted credential was copied. Dependency installation added 407 packages with zero reported vulnerabilities. This is an intermediate presentation review, not the fixed-candidate production-mode browser gate.

At 1440x900, the initial home had an oversized title with an isolated final syllable, an oversized motorcycle, and low-contrast dark navigation text against a dark header. Its card hierarchy and wording differed from approved node 31:778. These concrete differences are part of UI-REV-4. At 390x844, the schedule dialog placed the calendar, 24 hours and 60 minutes together, producing nested scrolling rather than the approved separate hour/minute selection popups. Corrections were assigned before final visual acceptance. The browser's viewport-aware screenshot was used after its generic screenshot renderer showed a scaled stale-size capture; DOM dimensions confirmed the requested 390x844 viewport.

Second runnable snapshot: 390px editor is visible in normal flow and blank hour/minute choices open separate grids; 1440px home title and navigation contrast are corrected. Final closure still needs the complete candidate and automated regressions. Remaining home corrections concern a full-width mobile description below the title/motorcycle row and real responsive text instead of CSS-generated replacement wording. Time-grid cancel/Escape/focus must return to the preceding date panel. Desktop summary inspection exposed UI-REV-10, so presentation implementation continues before declaring a final candidate.

The next summary snapshot introduced the shared presentation component but retained the old outer two-column grid, nesting the entire new summary into its first column. Removing that obsolete parent grid is required; the shared component now owns summary columns. The keyless demo also incorrectly displayed an actual-route badge and blank endpoint names in that intermediate adapter. Preserve the explicit demo distinction and actual demo endpoint labels before closure. At intermediate widths, use the approved space-dependent stacking provision rather than squeezing card text into single-character columns.

## Fixed backend boundary verification

The writer froze the two migrations while continuing UI and test work. The lead verified exact bytes and applied them as `postgres` to the corrected, isolated `motocast_release_chain_20260917` database in the verified PostgreSQL 17.6.1.166 container on local port 55434. This completes a fresh fourteen-migration chain without resetting an existing database.

- Shared course migration SHA-256: `263891c0c3e2c7fe82c633dd0de9c8a62b0ba0fe4df7861c0f33fc6bdc544198`.
- Favorites migration SHA-256: `b554f595d08e5f817430fd67b8142cc2ec9f4495ef4ebaf9c662d3800c6e80b7`.
- PASS: auth/RLS/budget 35; live ACL 143; plan/collection/share 118; collection concurrency 9; route-finalization concurrency 20; recommended-route concurrency 49; OIDC handoff 11; optional meals 13; release-announcement concurrency 2. Total across these nine suites: 400 PASS.
- FAIL: invitation/budget concurrency suite, 5 PASS / 2 FAIL. Its successful-publication fixture directly inserted a trip without the new reusable course, so the new publication precondition prevented the expected one-winner result. The writer must provide a valid reusable course in that success fixture while retaining the existing exact-one-publication assertions and separate missing-course denial coverage.
- Supabase CLI 2.116.0 security advisors: no warnings/errors. Only the verified loopback database used `sslmode=disable`; hosted TLS policy is unchanged.

For two-connection suites, the execution harness substituted only the hard-coded `dbname=postgres` DSN with the dedicated database name before sending test stdin. Source and executed-input SHA-256, substitution count and individual results were recorded in the owner-private local validation directory. The original container database was not used as a fixture target. New favorites and private shared-course role/concurrency tests remain pending; these results do not establish full release acceptance.

Lead direct-RPC proof on the same frozen favorites migration: three additions returned slots 1/2/3, a duplicate returned slot 3 without increasing the count, a fourth place was rejected with `FAVORITE_LIMIT`, a scalar JSON argument was rejected with `INVALID_FAVORITE_PLACE`, an outdated slot/place pair was rejected with `FAVORITE_NOT_FOUND`, exact deletion removed one row, and another active account could read zero of the first account's rows. PASS; both synthetic accounts and all favorite writes were enclosed in one rolled-back transaction. This does not replace the pending permanent regression/concurrency suites.

Lead concurrent-RPC proof: four separate sessions adding distinct places simultaneously yielded exactly three successes and one `FAVORITE_LIMIT`, with three rows in slots 1/2/3. Four sessions retrying the same place all returned slot 1 and left one row. Each transaction held its advisory lock for an additional 150ms to ensure contention. PASS for both scenarios. The exact synthetic account was removed in a final cleanup boundary and both account/favorite counts were verified zero. Source-input hashes and outcomes are retained in the private validation directory. No hosted fixture was created.

Both hosted targets passed CLI 2.116.0 `db push --dry-run --skip-vault`, listing exactly the shared-course and favorites migration timestamps above. No migration was applied to either hosted environment.

The writer corrected only the invitation/budget suite's successful-publication fixture to include the newly required reusable course. Lead rerun: 7/7 PASS with unchanged one-winner/one-share assertions. Corrected source SHA-256 `55625b3007de6f4d1e2dbd8271b986a585240acc285e37f3766f365fb017f944`; executed input SHA-256 `7bd8473395e65ab2e4cdfdf1801a2e49114ddd36404a84120400dfb2a9272e27` after four dedicated-database DSN substitutions. The ten existing database suites now total 407 PASS on the fixed backend; the earlier failed fixture result remains recorded above.

New permanent regression suites on the same frozen backend: favorites 19 PASS, favorites concurrency 2 PASS, expanded plan/collection/share 125 PASS and expanded live ACL 155 PASS. Reusing the eight unchanged suite results yields twelve suites / 447 PASS. New source SHA-256 values: favorites `0a046cda4247ef49716329e5cfb7baf0b9e342b4361897c0d5a9e899b563f9fb`; concurrency `c618a3d5a974c2a2d32a601e0c26862befd75472f81fdf0ad022ac912bef0c52`; plan/collection/share `304f981d5ff1a0ab3cc912bc1271471179979974874db231cdbfedf2718b6cae` (LF-normalized executed input `9860364ff75f9af9398a37d547182cc277b2c731fb5895e6fba7d99b68ff0df5`); live ACL `047f6a1af79fe4cd16b8477653654fdb060060efb12c66db608ce0797ae0b10b`. No observed failures or SQL errors. These tests add private-course authenticated/nonmember/revoked boundaries and favorite owner/role/slot/concurrency coverage.

The corrected retirement operator passed fresh read-only hosted state comparisons for both retained backups. Preview 118 and Production 119 target rows still match exactly. This is a dry run, not deletion.

Further UI-REV-10 review: the auto-requested collection-share preview was nested inside a closed parent dialog, so a successful preview RPC did not imply a visible approval screen. The writer must expose the sharing panel only in the current summary view, open its parent dialog for that valid automatic intent, preserve explicit publication, and avoid late navigation hijacking. Choice/share panels and preview stacking inside the bounded desktop modal are included in the existing sharing-screen contract. A real rendered visibility check is required, not only mocked child props.

Third runnable visual review: the corrected 768px summary uses one 689px content column; at 320px the home title stays on two intended lines, the motorcycle remains on the right with outer spacing, and home navigation focuses the heading. At 390px the date dialog rejected today's 00:00, retained its draft, accepted the next day's 00:00 and returned focus to the schedule trigger. These are executed keyless presentation checks; connected search/save/share and the full candidate production-mode baseline remain separate gates.

Writer full-candidate checkpoint: Vitest 588/588 PASS; lint, typecheck and build PASS. First Chromium baseline was 16 PASS / 16 FAIL / 2 connected-only SKIP across 34 cases. Failures included outdated summary/schedule selectors and release wording, plus two real regressions: hidden mobile invitation-management link and omitted legacy schemaVersion 1 desired/hard-return information. Writer restored those product behaviors and is adapting equivalent accessibility/layout assertions without adding skips or weakening conditions. The first browser run remains FAIL until the affected full rerun passes. Lead classifies the two actual regressions as UI-REV-12 and UI-REV-13 (MEDIUM, corrected but verification pending).

Final UI review found UI-REV-14 (MEDIUM, assigned): at 390x844 the pre-existing update-fetch error banner occupies y=710..828 above the new fixed navigation at y=774..832, hiding its buttons. This is a regression caused by the new mobile navigation. Offset only the mobile app-page error banner above the navigation, preserve its retry/update actions and other page/desktop behavior, and verify nonoverlapping rectangles plus actual navigation under the error state. Connected tests also require the new real UI flow before their Preview gate; keyless skip results are not connected acceptance. The lead requested equivalent map/metric nonoverlap, actual descendant weather-text font sizes and original primary-action contrast assertions to preserve existing verification meaning.

## Final candidate review and remaining gate

The next complete writer baseline passed Chromium 33 / skipped 2 connected-only tests / failed 0 (35 collected), Vitest 588 (71 files), lint, typecheck, production build (17 routes), five Deno entrypoints, operator unit 8, and diff whitespace checks. The two connected tests have been adapted to the real new dialogs and retain mutation tracking and exact cleanup. They remain NOT_RUN against hosted Preview. The frozen backend's twelve suites / 447 assertions and retirement operator's nine real PostgreSQL cases remain valid; no migration or operator bytes changed.

UI-REV-1 through UI-REV-14 are RESOLVED by source inspection and the affected regression checks above. The lead independently verified UI-REV-14 at 390x844: banner bottom 758, navigation top 774, no horizontal overflow, and successful navigation to the editor. The final responsive tests retain actual descendant font-size assertions, primary-action contrast >=4.5, and header/metric separation from the map. Original failures and correction history remain above rather than being relabelled as passed runs.

UI-REV-15 (HIGH, assigned) prevents final candidate approval: the connected editor has no rendered `action-notice`/`noticeRef`, because both remaining notice render sites are gated to summary or keyless mode. Route-provider, persistence, and input errors set state but remain invisible while the user stays in the editor. Render one focusable severity-aware notice beside the editor's calculation action, outside any disabled fieldset, preserve the summary notice, and remove any duplicate keyless-editor notice. Verify connected editor input/provider failures, visible error and focus without falsely navigating to a result; then rerun affected candidate checks. Hosted mutation and publication remain NOT_RUN.

An actual invited-rider login is also pending: both currently accessible hosted browser sessions were confirmed as administrator accounts. The user was asked about availability of a separate rider login while implementation and release preparation continue. Existing accounts/roles are not changed to manufacture this gate.

Lead review axes completed on the implementation boundary: API same-origin JSON checks, authenticated active ownership, bearer/proof nonexposure, stale-response isolation; transactional reusable-course publication/copy and favorite serialization; preserved motorcycle-safe provider requests and route/time validation; responsive/focus/error flows; additive schema/Edge/Web compatibility and exact-backup recovery. The 59 changed/new candidate files passed a value-redacting secret scan; no environment file, actual provider credential, private key, live share/invitation token, rider location or schedule is included. The temporary raw Figma exports were removed; exact outlined SVG assets are retained. Final staged/fixed-SHA checks remain required.

One cohesive release PR is appropriate: the approved new screens use private shared-course/bootstrap RPCs and favorites, and the backend must precede the corresponding Web artifact. The retirement operator is a manually invoked release prerequisite, never an automatic migration side effect. CI-only review and additive Preview rollout preserve reviewability without splitting into independently unusable product releases.

UI-REV-15 RESOLVED: the connected editor now renders one focusable severity-aware notice just after the disabled fieldset. It stays inside the editor form and does not duplicate the existing summary or keyless notices. Two connected-render regressions prove visible editor alerts and focus for missing schedule and provider failure, with one provider call and zero finalization on provider failure. The Preview auth helper now waits for the authenticated-only saved-routes navigation after login rather than the old default editor heading; its origin/project/private-file checks remain intact.

Final writer candidate: `/tmp/motocast-final-20260917`; verified code/assets manifest 55 entries, SHA-256 `6e99970514819f7abf3a70c378f9b02e29217a36a1861f5d07870a154998d6ef`. It includes both new API routes/tests and all three SVG assets; worktree code/assets mismatch count zero. Lead documentation is recorded separately and does not change executed source. Final results: Vitest 71 files / 590 PASS; Chromium 33 PASS / 2 connected-only SKIP / 0 FAIL; lint, typecheck, auth-helper syntax, production build 17 routes and diff check PASS. Five Deno entrypoints, operator unit 8, frozen PostgreSQL 447 and operator real-SQL 9 remain valid unchanged evidence. Lead review: BLOCKER 0 / HIGH 0 / MEDIUM 0 / LOW 0. This approves the CI-only candidate, not hosted or Production completion.

Staged-candidate verification covers 60 files and found no secret matches or unclassified/unstaged changes. The first staged whitespace check detected trailing blank lines in the three newly tracked Figma SVG exports (ordinary `git diff --check` had excluded untracked files). The lead removed only trailing CR/LF whitespace; exact trimmed bytes and parsed SVG XML compare equal to the writer-tested assets. Staged whitespace check then passed. The original writer manifest remains evidence for its exact source; the commit tree records these non-rendering asset whitespace changes and the final documentation.

## Connected Preview smoke on the initial released candidate

Exact source `1dd0997a199b6d8af848e494a6f8565b8da7c0cd`, Preview `dpl_8XPeELsAVmqVM37oJih1ZMgcCcBD`: authenticated administrator smoke passed current version announcement/footer, home/editor, actual provider search, three saved favorites and disabled fourth addition, shared favorites across place roles, ordered waypoint movement with stable numbering, separate date/hour/minute selection, real motorcycle-safe route and fresh persisted weather, explicit own-course save with visible success, server-built preview with zero published links before approval, explicit publication, same-summary public resolution with fragment removal, authenticated received-course copy, private new-schedule editor with blank date/time and preserved ordered points, visible missing-schedule error, and saved-course reapplication with blank schedule. This is administrator manual evidence, not the two instrumented connected tests or separate rider login gate.

UI-REV-16 (MEDIUM) was found on the new blank editor: passing zero map points led to an undefined SDK center and a false configuration/load error. Selecting the first verified place immediately displayed the real map, proving a blank-state rendering problem rather than a broken provider configuration. The writer corrected the map to show a neutral place-selection prompt when both points and path are empty; the existing real-geometry loading/error protections remain. Focused map tests 15 PASS, including empty-to-first-point transition; full final-candidate verification is pending. No DB/Edge/operator changes are required for this correction.

Manual test cleanup targeted the exact captured test IDs: one trip and two saved collections were deleted through owner-enforcing RPCs, three exact slot/place favorites were removed, and the issued test share was revoked. Independent readback verified those trip/collection/favorite counts zero, share revoked true, and accounts 2. Reopening the issued link displayed the expected unavailable/revoked error. No private rider itinerary or bearer is included in this report.

The attempted connected responsive sweep did not actually resize the background tab: DOM readback remained 1109px for each requested size. It is NOT_RUN for the requested connected sizes, not a pass; the deterministic production-mode responsive results remain valid separately. Vercel connector log access returned 403; the existing CLI 59.11.7 session successfully queried this exact Preview deployment after smoke and returned zero runtime error entries. Logs remain in owner-private storage. Supabase hosted advisors reported expected deny-all-table INFO and executable-definer RPC WARN categories plus the existing password-auth advisory; these are not a zero-warning result. The RPC exposure is intentional and covered by the exact live ACL/ownership tests. No security setting was weakened in response to advisory output.

UI-REV-16 RESOLVED on the corrected candidate based on `1dd0997`: Vitest 71 files / 591 PASS, lint/typecheck/production build 17 routes PASS, Chromium 33 PASS / 2 connected-only SKIP / 0 FAIL, and diff check PASS. The two-file verified manifest SHA-256 is `d08d13fe4a6281f500a668b8f831b9475fdd4351a990a24971bacd2f0efeda39`; map component `3ef04132c0dc655037c9021a90061398640fe68c489dafccd84ba077ae495d0f`, map test `62ac5a97dddc95c41ba4ee934ea75a29d7b2d75d1e1c65a762b9d6b46148fadf`. The backend, Edge, retirement operator and auth helper are unchanged. Lead inspection confirms no empty SDK center or fake geometry and preserved real-geometry failure behavior. This candidate needs exact-head CI and Preview redeployment; no second history retirement is intended.

## September 17 authenticated and Galaxy S23+ follow-up

Candidate `8f44f8f7082268da4c7a92d48add5bc6b168c919` reached develop Preview `dpl_3NjRM25Xmi5tz2TXX8m3suKrBE7H` (READY). Review/develop/promotion CI and the actual Vercel context passed. Promotion PR #62 remains draft. Production remains on `65c467f19d0ea39807123a0379d811ba61b72011`; its history retirement, new migrations/function, Web promotion and release tag are NOT_RUN.

The user completed a fresh Kakao login through the repository auth helper. The owner-private, repository-external state and metadata use mode 0600 and bind to the exact Preview origin/project. Independent membership readback identified this account as administrator. The user deferred the separate rider login until a later session; its helper was closed without creating a rider state. Do not relabel the administrator session as invited-rider or cross-user acceptance.

The two instrumented connected tests on `8f44f8f` returned **2 FAIL**. UI-REV-17 (MEDIUM) is a real accessibility regression: `selectedWeatherAnnouncement` is rendered only in the old nonconnected panel, not the new summary, so the expected weather live status is absent despite successful route/weather results. Restore the same live announcement in the current summary and verify loading/ready/stale states. The second failure is an ambiguous test selector matching both `저장한 경로` and `저장한 경로 보기`; scope navigation to its exact accessible name without weakening ownership assertions. The cleanup hook completed; subsequent readback found no collections or active shares. One remaining trip predates this test by several hours and was preserved, not treated as a disposable fixture.

The user additionally requested Galaxy S23+ sizing. The browser-emulation profile is 384x832 portrait and 832x384 landscape at DPR 2.8125 ([Blisk device profile](https://blisk.io/devices/details/galaxy-s23-plus)); Samsung lists the physical display as 1080x2340 ([Samsung specifications](https://www.samsung.com/ae/smartphones/galaxy-s/galaxy-s23-plus-green-256gb-sm-s916bzgbmea/)). This is browser emulation, not physical Android/Chrome/Samsung Internet verification. Actual Preview DOM readback confirmed both requested viewport dimensions. Portrait home/editor and full-screen search have no horizontal document/dialog overflow; search cancellation returns focus. Landscape schedule bounds are x=24..808/y=24..360 within 832x384, with an internal scroll container. Selecting hour 23, minute 59 and a future date, applying through the bottom button, cancelling and returning focus all succeeded. The browser was returned to home and its viewport override reset. Automated summary/weather/dialog tests for these two orientations and UI-REV-17 are in progress.

UI-REV-17 RESOLVED in the candidate based on `8f44f8f`: the current summary now contains exactly one weather live-status announcement. The connected-render regression proves loading, fresh and stale-provider-failure announcements. Lead review confirmed that existing sharing readiness/ownership checks are unchanged and the navigation-selector correction preserves all assertions. Writer source in `/tmp/motocast-s23-20260917` matches the five changed code/test files. Vitest 71 files / 592 PASS; Chromium 35 PASS / 2 connected-only SKIP / 0 FAIL (37 collected); lint, typecheck, production build 17 routes, and whitespace checks PASS. Both new S23+ tests passed with DPR/touch/actual viewport readback. The connected resize loops retain the original four dimensions and add both S23+ orientations without extra provider calculations. Hosted connected rerun remains NOT_RUN for the corrected source until exact-SHA Preview deployment. Frozen DB/Edge/operator evidence remains valid; no corresponding source changed. Lead candidate review: BLOCKER 0 / HIGH 0 / MEDIUM 0 / LOW 0.

A final test-only correction aligns the live share-preview assertion with the confirmed `예상 도착` metric, checking both the exact label and nonempty value in the actual summary metrics. Deprecated desired/hard-return and omitted-point assertions remain unchanged. Scoped ESLint, typecheck, test discovery (one intended live test), and diff checks PASS; connected execution still awaits the exact Preview. This does not change the product source or the previously executed keyless/unit/build evidence.

## Reopened Figma fidelity acceptance

The user rejected visible differences from the approved mobile and desktop Figma and explicitly requested correction of fixed labels that wrap to two lines on mobile. Candidate `b12a51d` and CI-only PR #63 are not approved for Preview promotion while this work remains. Prior behavioral test results do not establish visual equivalence. Figma is the reference; do not alter it to justify implementation differences.

Current plan:

1. DONE: load current Figma contexts/screenshots and map normal, empty, error and popup states to the production components. Reference file `wVNriNWb1OlF21DVq8rqlJ`, mobile page `0:1`, PC page `31:777`.
2. IN_PROGRESS: self-host the actual Noto Sans KR typeface and implement the home/header reference (`20:311`, `31:778`). Restore the mobile saved-routes title/button row and move its explanation to a separate full-width row.
3. IN_PROGRESS: restore editor/place/favorites/waypoint/date/time layouts and interactions, preserving provider validation, mixed-role limits and focus behavior. Focused behavior tests passed; lead visual findings below remain open.
4. IN_PROGRESS: restore result/received-result, saved-route and share/save layouts while preserving explicit preview/publication and owner controls.
5. PENDING: compare matching-content rendered screenshots after fonts settle. At 320, 384, 390, 820 and 1440px plus S23+ landscape, assert fixed-label line count, readable text, no clipping/overlap, internal scrolling and complete screen transitions. Deliberately multiline headings and variable-length place/address text must remain distinct from fixed single-line labels.
6. PENDING: run the final candidate baseline, review the fixed SHA, update CI-only PR scope, release only the verified Preview and rerun connected administrator scenarios. Separate invited-rider login and Production remain deferred by the user.

Confirmed fidelity gaps: the font is named but not loaded; home `전체 보기` can wrap; editor title, schedule position, waypoint actions and footer differ; mobile schedule is a sheet instead of a full-screen page; collection management forms remain in the primary list; summary metrics precede the map on mobile and use a different grid. These are MEDIUM UI fidelity findings, still OPEN until actual rendered verification. The browser does not render Figma's illustrative OS status bar, fake example dates/places, or illustrative map geometry as real data. Existing live safety/status/error and ancillary account-management capabilities remain available without replacing the approved primary layout.

Fresh baseline DOM readback on the existing Preview confirms `document.fonts` has zero loaded faces while the computed family merely names Pretendard/Noto Sans KR. The current user's editor draft was preserved. At actual 384x832, its two empty pass-through waypoint labels, schedule trigger and calculation button each occupy one line; this limited state is PASS and does not resolve the separately observed home-button wrap or other content/state combinations. The viewport override was reset after measurement. CI run [35159679454](https://github.com/tocomboy/motocast/actions/runs/35159679454) passed exact `b12a51d`; that older behavioral candidate remains unpromoted because the fidelity findings are open.

One procedure clarification was sent asynchronously: Figma's immediate link-copy sheet omits the existing SHARE-001/SHARE-003 complete server-built preview followed by explicit publication. Until the user changes that confirmed contract, retain the current approval boundary; independent typography/layout/save/list work continues. No implicit publication is authorized by a visual fidelity request.

Foundation/home checkpoint (uncommitted, not a release): self-hosted `@fontsource-variable/noto-sans-kr` 5.3.0, plain brand, mobile global navigation removal, home heading/button-row correction and mobile save-help card. Writer focused Chromium 11 PASS, related unit 2 PASS, lint/typecheck PASS. The assertions use loaded font faces and text Range rectangles: mobile home heading two lines, PC one line, and `전체 보기` one line across 320/384/390/820/1440. Lead visually inspected the actual local keyless app at 1440x1024 and 384x832, including 274px desktop hero, 224x210 motorcycle, 28px mobile title and two-line description. Account-backed recent cards remain a separate connected/fixture check; keyless emptiness is not populated-user evidence. Lead review requested removal of the noninteractive heading's accidental focus outline and unrelated npm lockfile churn; preserve control focus outlines and the original non-font dependencies. Editor, popup, result and collection fidelity remains IN_PROGRESS, and full integrated verification remains NOT_RUN. PR #63 was converted to draft while these corrections proceed.

Additional lead-rendered home finding: with a 320x800 desktop browser viewport reserving a 15px scrollbar, the mobile description inherits the hero's 20px right padding. Each line container is only 245px wide; the first sentence uses one text Range line and the second uses two. Figma's description is a separate full-width sibling. Restore that hierarchy and test the two intended lines using actual text rectangles; do not reduce the reference 16px font. The previously passed title/button assertions do not cover this newly reproduced description failure.

Editor/dialog checkpoint: writer lint/typecheck PASS, focused unit 6 PASS, S23+ place/manage/register/back/ESC/focus and landscape nested schedule focus Chromium 1 PASS. Eighteen screenshot files were successfully generated from production components at 384/390/1440; capture success is not visual acceptance. Lead inspection found OPEN fidelity gaps in the captured candidate: the received-course planner retains the old shared wrapper/header around the new app header, the mobile editor uses a left arrow plus right-aligned title instead of the reference title plus right home button, the selected-date bar remains below the time controls, and cancellation/manage controls retain native browser styling. The date apply action is not full width. Owner-keyless DOM at 384x832 independently confirmed the header `space-between` alignment and a 917px document caused by an 832px app shell plus the release footer. These are assigned to the writer together with the existing home-description correction; no hosted promotion or final visual PASS is claimed.

Sharing procedure remains unchanged for this visual-fidelity slice. The optional clarification has not produced a new decision; the existing SHARE-001/SHARE-003 server-built preview followed by explicit publication is therefore the implementation baseline, not an unresolved prerequisite for independent UI delivery. A later request to remove that approval step would be a separate product-contract change. The Figma-style choice/save/share presentation must not auto-publish a link.

Second rendered fidelity review remains FAIL/OPEN, not a release candidate: the newer 384/1440 editor captures still showed both desktop and mobile calculation labels, an inherited public wrapper double gutter, favorites inside the right map panel instead of the left fixed footer, and a selected-route heading below the map. The received-summary mobile map remained 390px because earlier min-height rules defeated the 164px height. This repeated style-cascade cause requires consolidating the affected presentation rules rather than adding more competing overrides. The lead authorized that bounded consolidation and specified a single owner for each shell's gutter, one base/breakpoint rule group, pinned schedule/favorites/action outside the place-list scroller, and separate normal-data versus omission/error captures. Full baseline is held until this rendered correction boundary passes. Writer focused unit 20 PASS and S23+ landscape regression 2 PASS are behavioral checkpoints only; the development-server service-worker check is deferred to the required production-mode browser baseline, not marked passed.

Third rendered checkpoint: compact waypoint cards, a single calculation label, editor title above the two-column layout, 164px public-summary mobile map, stacked total/driving/rest metrics, and date/calendar/time hierarchy are now visible in actual-component 384/1440 captures. This is partial acceptance, not final visual PASS. Remaining bounded differences are the PC calendar close-button position and equal column widths, native-styled secondary controls, 56px calculation button versus the 48px reference, waypoint role alignment, and the still-duplicated owner-summary route box. The screenshots named `fixed-home-*` actually contained the keyless summary; they are not home evidence. Correct home and populated collection/share/save captures remain required.

Lead code review also identified two UI regressions to close before the baseline: mobile selected-place clearing is nested inside the initially hidden search-results section; collection operation failures are visually hidden by the generic ready-state status class. The writer is moving the clear control to the common picker area and separating operation feedback from list-loading status. Existing provider, ownership, explicit publication, and draft-date contracts remain unchanged. A manual 320px read was invalidated by development hot reload and is NOT_RUN; the viewport override was reset. Freeze the updated review source before the next lead visual pass.

Main-layout visual review now accepts the corrected home hero, date layout, compact cards, summary hierarchy, visible place-clear footer and search-connection error state. Three bounded presentation corrections remain: keep the waypoint role in the heading row so it does not narrow the place/address column; render collection/save evidence inside the actual production parent layout and explicitly restore collection action contrast; match the mobile time list's Figma 240px internal scrolling area. The lead permits the final baseline immediately after these corrections and the focused regression pass, concurrently with the last visual review of that same frozen source. The baseline must come from a fresh repository-source copy, excluding the external `/fidelity` route and mocked Supabase module; the expected production build remains 17 routes. Existing unit checkpoint is 6 files / 21 PASS and Galaxy Chromium 2 PASS. These are not the final full baseline or hosted verification.

The final production-mode browser baseline completed with 38 PASS / 2 connected-only SKIP / 0 FAIL (40 collected), while Vitest completed 72 files / 597 PASS, ESLint/TypeScript/five Deno entrypoints/build 17 routes PASS. The initial obsolete-selector/mock failures were corrected without treating their failed runs as passing; the final writer record will identify the exact candidate and rerun boundary. Subsequent positive-search rendering reproduced a real fixed-label overflow: the 104px mobile selection button inherited excessive horizontal padding. The correction preserves 16px text and one-line labels with bounded mobile padding. The same positive-search screenshots now show readable selection labels and all three shared favorites. A manage-mode width specificity defect remains under correction before the manifest. Synthetic collection/choice wrappers that omit the actual parent controls are not whole-screen visual evidence; full connected home/collection/save acceptance is reserved for the exact candidate Preview. The baseline is local deterministic evidence, not connected or Production GREEN.

Final pre-commit fidelity checkpoint: the writer's 31-file manifest `d64187555487fd1e7296cbf1f52af23379c18d245d2dbad7809740a804e05b95` matched every worktree implementation/test byte; the lead research document was the only separately owned changed file. Value-redacting source secret scan covered 32 files and reported zero matches. Local final baseline remains 597 unit PASS, 38 Chromium PASS / 2 connected-only SKIP / 0 FAIL, lint/typecheck/five Deno/build 17 routes PASS, plus affected 17 browser PASS and actual place-label Range assertions at 384/1440 PASS. This evidence does not mark the connected-only tests as passed.

Final manage/register crops exposed a remaining bounded fidelity defect: generic favorite-button styling defeated the dark register action and the mobile manage screen duplicated its completion control instead of pinning the bottom action. Lead requested the exact 6:111 / 55:4781 layout and explicit register-result button styling, preserving the maximum-three and provider contracts. Static review also found that the skipped hosted tests still referenced retired titles, nested save dialogs, and hidden management controls. These tests are being aligned with the current production navigation while retaining provider counts, explicit publication, and exact test-owned cleanup. Neither finding is hidden by the already-passed deterministic baseline; exact Preview delivery remains pending their correction and CI.

The final collection review uses separate desktop and mobile reference hierarchies (31:781 / 28:760), with the ancillary share-management controls retained in a collapsed section. The lead also reproduced a new display-only midnight regression in `formatSummaryDeparture`: Node 20 formatted `2026-09-20T00:00:00+09:00` as `9/20, 24:00` under the English locale and `hour12:false`. A bounded `hourCycle:"h23"` correction and midnight regression assertion are required before candidate freeze; no schedule or provider calculation contract changes.

The 32-file follow-up was not approved for promotion: the final 1440px manage crop still inherited the search screen's two-column grid, squeezing its redundant right-column heading into individual vertical characters. At 384px the completion button remained mid-page. Capture-test success only meant execution, not visual acceptance. The lead traced the repeated cause to manage/search layout coupling and specified a structural correction: remove manage's redundant results section, use one scrollable content column, move completion into a separate footer, and constrain the mobile shell to viewport height. The affected verification must assert desktop column width/title line count and the mobile completion button's bottom coordinate. This replaces further incidental overrides and keeps the candidate held until those actual-component checks and lead image review pass.

## Frozen fidelity candidate and CI-only handoff

The final 32-file writer manifest is `94c32534d15d2cd00d1ac70d2d7a7aadb57c07c98fe8b7b4dbd4f9e3b67a2646`; lead verification found zero source-byte mismatches and only this research document outside the writer set. The 33 changed files passed the value-redacting secret scan with zero findings. The final manage/register 384/1440 crops are visually accepted after structural layout separation and explicit footer button styling; desktop title one line, 720px dialog, mobile viewport-minus-20 completion position, independent scrolling, full-three disabled registration and two-of-three enabled registration were asserted on the actual component. The final scoped browser check is Galaxy 2 PASS, following the structural 17-browser PASS and place-unit 3 PASS; the preceding collection/Galaxy/planner impact run was 18 PASS / connected-only 1 SKIP, and focused unit 10 PASS plus lint/typecheck passed. The final whole-source CI must run before hosted promotion. Earlier full baseline 597 unit PASS / 38 browser PASS / 2 connected-only SKIP / 0 FAIL, Deno five entrypoints and build 17 routes are retained with their exact rerun boundary, not relabelled as hosted evidence.

Fidelity implementation steps 2–4 are complete. Step 5 is complete for local home/editor/summary/date/time/search/favorites/fixed-label and scrolling evidence, with full connected collection/save/choice acceptance reserved for the exact Preview. Step 6 is now CI-only fixed-commit review and Preview release/administrator verification. No backend, operations retirement, account role, CI policy or Production change is part of this follow-up. Separate invited-rider login and Production remain user-deferred. Fixed commit/CI/deployment and connected results will be recorded on PR #63 and promotion draft #62 and in the linked Notion task to avoid changing a tested artifact merely to embed its own SHA.

## Connected Preview fidelity readback and bounded correction

PR #63 delivered exact commit `372f012c6d32d9d509b70be095f07e3e3b5b5180` after CI-only run 35172022525 passed (598 unit PASS, Chromium 38 PASS / 2 connected-only SKIP / 0 FAIL, lint/typecheck/five Deno entrypoints/build PASS) and zero GitHub Deployments/Vercel checks were read back before fast-forward. Preview `dpl_5r6pcb4EyE21w6V2EPJ6FpAAGyb5` is READY on the verified project/develop SHA; HTTP/security headers PASS and post-deploy error-log query returned zero entries. Production remains unchanged.

Actual authenticated Preview review found a residual MEDIUM presentation regression: legacy `.collection-manager` framing adds an empty outlined home box and duplicate gutters/outer border around the standalone saved-route list. Desktop also retains a redundant heading CTA absent from Figma 31:781. The fixed follow-up removes only the nested manager framing, aligns the standalone container to the home gutter, and removes that redundant CTA; actual item cards, operation errors, and header navigation remain. The same framing reset applies to the save panel inside its owning dialog. These findings remain OPEN until rendered verification of the corrected source.

Connected execution on 372f012: owner-management test PASS; live route/collection/share scenario FAIL before its first mutation because its selector looked for the old visible `출발지` label while the Figma component displays `출발` and retains accessible name `출발지`. No trip/collection/share cleanup obligation was created. Fix the test to use the current accessible trigger name and rerun the full scenario. Actual S23+ 384x832 date controls and ten positive provider search-result selection buttons each had one text line and no horizontal overflow. This is bounded rendered evidence, not full connected completion.

Post-fast-forward develop push CI 35172181200 reported five missing-element failures in static invite markup tests, although the same-SHA PR CI 35172183763 passed. The helper replaces the body of a still-hydrating Next page, allowing hydration to discard its fixture DOM. Stabilize this static production-component layout fixture with the real compiled stylesheet in a script-free document, retain every assertion, and rerun the affected suite and mandatory baseline. Do not classify the failed run as passing.

Plan: finish these bounded presentation/test corrections, verify the new fixed candidate through a new CI-only PR, fast-forward exact SHA to develop, then recheck authenticated Preview home/list/save and the complete live administrator flow. Separate invited-rider and Production verification remain deferred; no backend, data-retirement or role changes are authorized by this follow-up.

The bounded correction also restores PC search node 35:952: a 960px dialog with 32px inner padding, a 280px favorite card separated from results by 24px, the role-specific title and a bottom favorite-management action. The mobile search composition remains separate. Lead image review accepted the sidebar/title/mobile error and clear-place structure, and identified one local regression from removing desktop search padding: the absolutely positioned magnifier retained its old top offset. Center both desktop search/clear buttons inside the input and assert their bounds before final acceptance. Normal positive-result visual acceptance remains reserved for the final Preview provider response.

Static invite fixture diagnosis is resolved by a script-free same-origin intercepted page under the service worker's existing excluded `/admin/` namespace, with the actual compiled stylesheet links and loaded Noto font assertions retained. The intermediate blank-origin/font and intercepted-navigation setup failures are not PASS. The final invite responsive suite passed all ten cases on two consecutive runs; focused Galaxy/collection evidence is 3 PASS / 1 connected-only SKIP. Current unit baseline is 72 files / 598 PASS with lint/typecheck PASS. Remaining mandatory browser/build/Edge checks and final source identity follow the writer manifest; no hosted outcome is inferred from these local runs.
