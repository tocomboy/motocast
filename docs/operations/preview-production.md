# MOTOCAST Preview and Production operations

This runbook implements `OPS-002`, `OPS-004`, `OPS-005`, and `OPS-007` from `docs/product/MOTOCAST_SOT.md`. It records project references and secret ownership, never secret values.

## Latest connected verification — 2026-09-06 KST

The final negative-weather sharing run passed11, followed by exact cleanup readback PASS and an unchanged nonfixture fingerprint. Missing, stale, and naturally expired seeded weather each caused the actual Preview preview/publish RPCs to reject sharing. This closes the six bounded database cases; it does not prove active-provider failure, budget exhaustion, Kakao browser completion, or Production readiness. [Final connected evidence](../work/research/2026-09-06-negative-share-connected-proof.md). The two failed earlier runs below remain preserved history; all their test resources were cleaned.

The subsequent negative-weather sharing fixture run recorded PASS3/FAIL2/NOT_RUN6. Its private-builder precheck hit management read-role permissions; cleanup then stopped on catalog representation differences between management roles. A separately reviewed recovery pinned `search_path=public` inside the existing guarded cleanup transaction and removed the exact test-owned public16 rows and temporary Auth identity. The second run recorded PASS4/FAIL1/NOT_RUN6: corrected setup and automatic cleanup passed, but PostgreSQL timezone text parsing stopped the pre-RPC expiry wait. Both runs have independent absence/fingerprint readback PASS and remain failed. No real-account, provider, budget, application permission, or Production change occurred. [Execution and recovery evidence](../work/research/2026-09-06-negative-share-execution-contract.md), [remaining gates and interview inputs](../work/research/2026-09-06-preview-remaining-gates.md).

The explicitly approved temporary Preview Auth fixture execution passed149, with independent APPROVE B0/H0/M0/L0 and exact cleanup readback PASS. This closes the tested actual JWT owner/cross-owner/revoked/nonmember/anonymous boundaries and fixture invitation claims. It does not close Kakao browser completion or provider/budget/weather-sharing failure gates. Existing real accounts were preserved; nonfixture public16 tables and auth.users fingerprints matched. [Exact evidence, taxonomy and limits](../work/research/2026-09-06-preview-auth-connected-proof.md).

GitHub confirms develop98afe02 and both existing exact-SHA CI runs success; active Preview Edge versions are unchanged. Vercel alias verification initially failed because the stored access token had expired, with no local project binding available. Cached official CLI59.11.7 whoami refreshed the existing session; team-scoped API readback now confirms dpl_HiueyMFkC1vL2NRfJncJNEst8ECm READY at exact98afe02. Use the normal CLI refresh path when an existing OAuth session expires; never put credential values in scripts, logs, or commits. New releases still require their own settled no-deployment and exact-alias evidence. No new application release, main/Production mutation, region change, billing change, or provider call was performed.

## Historical readback — 2026-09-05 12:38 UTC

Preview product `4f324854874181b9e139c5d163d7368bf3861fa5` is deployed and independently approved. PR #23 exact-head CI `33966249264` and develop CI `33966431080` succeeded; settled GitHub/Vercel deployment/check/status counts were zero before the unchanged `0b23aea` base fast-forward. Vercel `dpl_7KPdHQbCddRPsKWbK9hA1TKhFTWj` and the actual develop alias match this SHA. The display now expires at `validUntil`, matching the existing sharing cutoff. Edge versions remain weather-timeline12, plan-route11, and the other three8. The deployed UI expiry check passed with an injected share response; it is not provider-failure or server share-expiry proof.

The current Production alias `motocast-three.vercel.app` resolves to `dpl_7c5UdGv4VWcs4k1YwHNERhQZVuH8`, READY, target Production, Git source **develop/201e1ec12c967da57fb671fad294cf1d05b9d56c**. Remote main remains **d0134ed93d7e0d8aed1123c5d693c665bbe646e8**. These are different facts. Historical statements below combining “Production/main d0134ed” do not prove the deployed Production Web SHA. This existing lineage conflict with OPS-003 is recorded for the concrete Production approval interview; neither the alias nor Production was changed. The configured Production branch is main, but changing that setting in the past did not establish a new main deployment.

Production Supabase remains Tokyo and has three applied migrations through `20260830212000`, eleven public tables, and search-places/plan-route/weather-timeline v3. It must not be treated as empty. Preview remains Seoul with all eleven migrations. Read-only access checks passed11 and the current ACL subset passed103; actual A/B/revoked/nonmember login and provider/budget failure gates remain open. See [current release evidence](../work/research/2026-09-05-preview-expiry-release.md), [gate matrix](../work/research/2026-09-05-preview-gates-plan.md), and [unapproved Production packet](../work/research/2026-09-05-production-promotion-packet.md). Older sections are chronological evidence, not the current overall completion state.

## Environment boundary

| Environment | Supabase project | Region | Vercel deployment | Access boundary |
| --- | --- | --- | --- | --- |
| Preview | `lehjmbgfpoemqcwxowbx` (`MOTOCAST_Preview`) | Seoul `ap-northeast-2` | `develop` Preview | Vercel Authentication plus MOTOCAST test invitation |
| Production | `obodvbyzptxeehgpcpkd` (`motocast`) | Tokyo `ap-northeast-1`, retained under `OPS-008` | `main` Production | MOTOCAST invitation and Kakao login only |

Never copy Auth users, memberships, trips, collections, shares, provider secrets, budget ledgers, or production backups between these projects. Migrations and Edge Function source are the only shared artifacts.

## Vercel project

Project: `tocomboys-projects/motocast`.

- Production Branch: `main`.
- Node.js: `20.x`.
- Vercel Authentication: `preview` only.
- Production variables point to the final Production Supabase project.
- Preview variables point to `lehjmbgfpoemqcwxowbx`.
- Each scope keeps only `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and `NEXT_PUBLIC_KAKAO_MAP_JS_KEY`.
- Do not keep service-role, Kakao REST, KMA, allowed-origin, or budget values in Vercel.

Read back project settings without printing environment values. When environment entries are audited, print names, targets, and IDs only.

Verified Preview configuration on 2026-08-31: the three public names above exist as `Preview (develop)` Config entries. A deployment-level pull exposed seven older server-only names that had been mistakenly targeted to both Preview and Production; after user confirmation they were Preview credentials rather than created Production credentials, all seven were removed from Vercel. No server-only/provider/budget name now remains in either Vercel environment. Production credentials are not yet created and remain deferred until separate post-Preview configuration approval; `OPS-008` already confirms the existing regions.

## Kakao Map and Local activation

The JavaScript key and allowed web origin are necessary but do not activate the Kakao Map product. For each environment, use the matching Kakao app and complete this check without exposing a key:

1. In Kakao Developers, open the environment app and go to **Kakao Map > Usage settings**.
2. Before changing **State** to ON, confirm the dashboard identifies the app as eligible for the Kakao Map free quota and does not require a Biz Wallet, paid API, or automatic billing. Under `COST-001`, stop and interview the user if any paid setup is required.
3. Under the app's JavaScript key, register the exact JavaScript SDK domain. Preview uses `https://motocast-git-develop-tocomboys-projects.vercel.app`; do not use a comment URL or a deployment path.
4. Confirm that the Vercel Preview Config name `NEXT_PUBLIC_KAKAO_MAP_JS_KEY` contains that app's JavaScript key, not its REST API key. Never print the value.
5. Redeploy or refresh the fixed Preview alias and verify that the SDK request returns JavaScript successfully. A `403 NotAuthorizedError` saying the app disabled `OPEN_MAP_AND_LOCAL` means activation is still incomplete; it is not an application success and must not fall back to demo data.

On 2026-08-31 the fixed Preview alias initially returned that exact 403 because `MOTOCAST Preview` had Map/Local disabled. After the user confirmed the free-only activation, a key-preserving request from the fixed Preview origin returned `200 text/javascript` and the authenticated browser rendered the Kakao map. The client-side error boundary retains its ten-second timeout and safe configuration error. The first real place search then exposed a separate response-contract defect: Kakao officially returns `http://place.map.kakao.com/...`, while the browser accepts only the HTTPS Kakao host. The server correction validates that exact host and upgrades the URL to HTTPS; do not weaken the browser host check or accept arbitrary provider URLs.

Before an actual route response exists, the connected map may show selected markers but must not connect them with a synthetic straight line. After calculation, the only route polyline source is the ordered Kakao Mobility road `vertexes` returned and validated by `plan-route`. Origin, destination, lunch, dinner, rest, and mandatory pass-through waypoint markers use distinct lettered images and a non-color legend; their roles come from the accepted route occurrence and must not change when the rider edits the next calculation's form. Preview smoke must confirm those roles and complete bounds at mobile and desktop widths. Exact-coordinate roles use one composite marker so neither letter is hidden by pin stacking. Every new recommended route must traverse every selected waypoint in the exact visible mixed-role order. An immutable share created under the deprecated candidate-specific policy can still contain a legacy winding-only point absent from its balanced or shortest geometry; that legacy point carries a `×` marker symbol and remains labelled `선택 경로 미통과` in an always-visible notice outside the fixed-height map. The compatibility notice remains readable when the SDK is loading or failed and must contain all supported omission rows without clipping at 320, 390, 820, or 1440 pixel widths. Planner and shared-view status, role legends, route metrics, and plan controls remain in normal flow outside the map, except for the bounded loading/error status and the map's own location control. Regression evidence must exercise the actual shared snapshot renderer or public share route and measure overflow before any scripted scroll.

## Single recommended route

`plan-route` sends exactly one `RECOMMEND` request per provider chunk through the rider's complete ordered point list. Every request fixes `car_type=7`, `avoid=motorway`, `roadevent=0`, `summary=false`, and detailed geometry; it never sets `alternatives` and the browser cannot submit candidate or safety policy fields. Every attempted provider request consumes budget under `COST-002`.

Edge admission and database persistence use the same `0.005` degree tolerance only for normal Kakao road snapping between a requested place and the first/last road point. Road-to-road and section-to-section continuity remains the stricter `0.0002` degree boundary. Both layers reject points outside those bounds; persistence also revalidates every road scalar, vertex, aggregate, occurrence role, and route-plan identity before atomic finalization.

A waypoint is a rider-authored mandatory zero-dwell pass-through point, not an automatically detected winding route class. The planner authors waypoints, lunch, dinner, and rests in one list; all provider chunks must retain that exact order and each dwell. If Kakao cannot return a safe route through the approved points, the request fails explicitly; never request a winding alternative, generate an arbitrary detour, draw a straight line, or use a passenger-car fallback.

The trusted request boundary rejects more than twenty selected mandatory pass-through waypoints, more than one lunch or dinner, more than five rests, or more than thirty total occurrences before consuming provider budget. Edge and database validation both use `0.005` degrees only for a requested place to snap to the provider road endpoint, while actual road-to-road continuity inside and across split legs remains `0.0002` degrees. Finalization locks the drafts, compares their canonical plan and recommended-route hashes with the durable planning tombstone, and revalidates geometry before persistence. Current schemaVersion 3 share projection preserves the accepted occurrence `stopRole`; immutable schemaVersion 1/2 rows are never rewritten and continue to use the legacy reader fallback. The physical `winding` field remains a compatibility marker for the mandatory pass-through role, but current UI, map and operational language call it `경유`.

The `PLAN-003` ordered-waypoint release was completed as a coupled Preview contract at exact SHA `57cfa83`. Migration `20260902123000_ordered_waypoint_limits.sql` changed no table and rewrote no stored row; it replaced the two trusted validators so a forged `winding=false` value cannot bypass the semantic 20-waypoint limit. The release deployed the reviewed `plan-route` and `save-collection` writers, preserved a deliberately non-regrouped `lunch → waypoint → rest` sequence through route requests, collection restore, and share legs, and passed the connected weather, publication, cleanup, accessibility, and viewport gate. Production and `OPS-008` remain unchanged.

Read-only release baseline refreshed on 2026-09-05: `origin/develop` is exact SHA `57cfa83a54020f80b577a48905fb1ff569842fd7`; develop-push CI run `33595074605` completed successfully for that SHA. GitHub Preview Deployment `6216171765` has successful status `17660300448` and points to `https://motocast-6qqi7v0fw-tocomboys-projects.vercel.app`. The explicit Preview project `lehjmbgfpoemqcwxowbx` has eleven matching local/remote migrations through `20260902123000`. Its active Edge baseline is `search-places`, `plan-route`, `weather-timeline`, `save-collection`, and `kakao-oidc` all at v8; the first four retain JWT verification and only `kakao-oidc` has it disabled. Do not use an implicit linked-project read: the local link can identify a different environment. Local `develop` contains an unpublished UI/accessibility follow-up above this baseline; it changes no migration or Edge Function and must complete fixed-SHA review, non-deploying PR CI, exact Preview deployment, and connected regression smoke before being treated as the next release.

## Whole-hour KMA correction completed in Preview — 2026-09-05

The user approved empirical request-selection evaluation and adoption while preserving exact request/response issuance binding. Two bounded Preview probes observed ultra 19:30→19:00 and 18:30→18:00, then direct 19:00→19:00 and 18:00→18:00 with strict and exact-target required-value validation passing. Short 17:00→17:00 passed in the first probe. These public fixed-grid observations do not prove every publication boundary or six-hour target is available. They replace the prior official-evidence-only blocker, not the exact acceptance guard.

CI-only PR #19 (0d18a27, run 33961430841) and #20 (b2d1e3d, run 33962475338) passed exact-head CI with zero predeployment GitHub Deployments/Vercel checks/statuses. Each separately deployed temporary function was invoked once, then deleted after matching owned ID/version/updated_at; both DELETE/absence readbacks were 200/404. Final function listings retained the original five versions and settings. Real budget reservations remain charged. Temporary source is removed from the product candidate. The final second-probe runner hash was 5ad3f7e49710802d77d28e1e57ed47b09763979bc199275022379671a3949965, independently approved with B0/H0/M0/L0; its eight mocked-I/O tests passed.

The deployed correction changes only ultra request minutes to :00 while retaining the :45 hour-selection gate, and requires its exact rounded target forecast group before required-value checks. Missing ultra targets follow existing provider failure/stale handling; short nearest selection remains for its documented three-hour extended intervals. URL, strict parser expectation and saved issuance use the same base. B1 keeps its historical official :30 schedule classification, so an empirical whole-hour request can be INVALID/INTERSTITIAL in that diagnostic while strict parsing succeeds. B1 is never acceptance input. No retry, offset acceptance, model substitution, schema, secret or budget-limit change is included.

Product SHA 8bd8878 completed writer verification, independent APPROVE B0/H0/M0/L1, PR #21 CI 33963499094 and develop CI 33963674030, and Preview rollout. weather-timeline is ACTIVE v12/JWT true; other functions are unchanged. Vercel dpl_2xWMSHiTArk75Hxt8qdAtKG7HEpt is READY on develop/8bd8878; GitHub Deployment 6280478531/status17866058512 succeeded. The same-public-input connected weather/collection/share/revoke/reissue test passed 1/1, failures/errors/skips/retries0, including exact owned-resource cleanup. Runtime logs show no unexpected errors; two revoked resolver404 responses are expected. The LOW implementation-pending wording is corrected in this documentation update. These results supersede the old f6ec59c weather blocker and all historical state entries below. Production/main remains untouched. Full evidence and validation limits are in [the release record](../work/research/2026-09-05-kma-hour-correction-release.md). See [the implementation contract](../work/research/2026-09-05-kma-hour-correction-plan.md) and [second probe evidence](../work/research/2026-09-05-kma-hour-observation.md).

## UI release history and original route blocker — 2026-09-05

This historical entry superseded the unpublished-UI and pending-review statements in the earlier baseline above. The later provider-diagnostic release below owns the current deployment and connected result. Exact Web SHA `840a8eb3d6a305ca8d5c64e424eefb9b820fc3be` passed independent UI/correctness/operations review with all four original MEDIUM findings resolved, CI-only PR `#12` run `33945857752`, and develop-push run `33946005523`. Repeated pre-promotion checks found zero Deployments and zero Vercel checks on the slash-free review branch. The unchanged develop base was fast-forwarded to the same reviewed SHA; Preview Deployment `6277321876`, status `17858050603`, succeeded at `https://motocast-34t9xwsv7-tocomboys-projects.vercel.app`. Production remains at `d0134ed93d7e0d8aed1123c5d693c665bbe646e8`; no migration, function, secret, or Production setting was changed by this UI rollout.

Authenticated access to owner management passed `1/1`. A read-only check at the fixed develop alias passed seven actual Kakao-canvas layouts (320/390/820/821/901/1000/1440), with external map information, unclipped facts, one route-summary heading, and page errors `0`. However, the complete connected mutation scenario failed twice before any finalization request. The bounded diagnostic rerun observed `plan-route` HTTP `502`; Preview logs at `2026-09-05T05:02:05.419000` and `2026-09-05T05:06:16.477000` reported `INVALID_ROUTE_PROVIDER_RESPONSE`. No trip finalization, collection save, or share publication was reached; provider budget consumption is not undone. These results do not prove that route calculation, weather, or sharing currently works, and this Preview gate is **not GREEN**. Stop repeated provider calls; investigate the rejected response field with bounded, sanitized evidence before changing any validation or deploying a backend correction. Never weaken road-geometry or motorcycle-safety validation to make this test pass.

## Bounded provider diagnostics

Writer verification for the prepared cohort slice: focused `5 files / 143 PASS`, full `58 files / 515 PASS`, lint/typecheck, Deno five entrypoints, build and deterministic Chromium `20 PASS / 2 connected SKIP`. Product test failures and setup/import failures are zero. An initial stale-copy run executed only the old `4 files / 111 PASS`; it is explicitly excluded from candidate evidence, then the exact source was synchronized and rerun. Unchanged lockfile/dependencies were reused. Review scope is four runtime files (new diagnostic module, parser attachment, sanitized error context, two handler log calls), four test files and three current-status documents; UI/route/DB/request policy are unchanged.

Current cohort diagnostic release, 2026-09-05: exact `f6ec59cee100147406f6582ded1caa568369e886` is on remote develop and Preview. PR #18 CI `33953581312` and develop CI `33957483057` pass. Pre-promotion Deployment/Vercel check/status counts were zero. Only explicit Preview `weather-timeline` advanced to ACTIVE v11 with JWT true; `plan-route` remains v11, other three functions v8, only `kakao-oidc` JWT false. GitHub Deployment `6279306497` / status `17863057265` is success; Vercel `dpl_Bv8N1ASjCTgP6AHRG483UGb7JsnZ` is Ready and both its deployment and fixed develop alias report exact f6ec59c. Production/main remains `d0134ed93d7e0d8aed1123c5d693c665bbe646e8`; no Production, DB, secret, provider-policy or paid-service change occurred.

The one unchanged-public-rest-comparison run is `PASS 0 / FAIL 1 / ERROR 0 / SKIP 0 / DESELECTED 0 / XFAIL 0 / SETUP_OR_IMPORT_FAILURE 0`. Route calculation/finalization/map/ordered legends pass, then weather fails at `2026-09-05T09:17:57.585000` with BASE_TIME_MISMATCH. Its B1 result is `B1 COMPLETE VALID ONE PRESENT,VALID,ONE,EARLIER_WITHIN_HOUR_TEN_MINUTE,INTERSTITIAL_TEN_MINUTE,MATCH PRESENT,VALID,ONE,EARLIER_WITHIN_HOUR_TEN_MINUTE,INTERSTITIAL_TEN_MINUTE,MATCH ABSENT,NONE,NONE,NONE,NONE,NONE`. Both known cohorts share one earlier interstitial issuance and matching grids; no unknown group or mixed issuance identity was observed. COMPLETE means all parsed items, not all remote pages. The exact offset/model and provider-selection cause are not known, and the periodic-refresh guide does not establish acceptance of this earlier response. On 2026-09-05 the user chose to preserve exact binding and confirm the official supplier rules; no replacement contract or repeated policy approval is pending. Keep the current guard while that supplier evidence is missing. The completed independent afterEach deleted the exact test trip; collection/share were NOT_RUN and budget usage is retained. Test artifacts are owner-private outside the repository; no bearer-producing share step was reached.

The existing source/tests/config in the writable verification copy matched 154 checked tracked files before reuse. Prior fixed-SHA writer/reviewer results were reused, not rerun: writer focused 143 PASS, full 515 PASS, Chromium 20 PASS / 2 connected SKIP; DB NOT_RUN for this diagnostic-only slice. The following preparation and c926198 release records are historical.

Cohort diagnostic implementation (now deployed at f6ec59c): after the unchanged parser has selected a base-related error, inspect only the already-parsed rejected array, capped at 1,000 entries, then rethrow that exact error. `kma-binding-diagnostic.ts` owns pure aggregation and a finite-token formatter. A WeakMap associates only the sanitized string with the error, leaving the error's message/reason/serialized properties unchanged. Existing handler terminal/stale logs add that optional summary; public bodies, stale writes, successful responses, request count, budget order and acceptance remain unchanged. No additional fetch, clock read or retry is introduced.

The server-only format is `B1 coverage expectedSchedule issuanceCardinality refresh known unknown`. Coverage is COMPLETE or TRUNCATED (all other claims apply only to the inspected prefix); global cardinality counts distinct valid issuance identities only as NONE/ONE/MULTIPLE. Each fixed cohort has six comma-separated finite fields: presence; binding shape; valid-issuance cardinality; relations to requested issuance; regular/interstitial-ten-minute/other schedule; grid match/mismatch/invalid. Relation bins are EXACT, EARLIER/LATER_WITHIN_HOUR_TEN_MINUTE, EARLIER/LATER_WITHIN_HOUR_OTHER and EARLIER/LATER_HOUR_OR_MORE, computed internally across complete calendar dates. Sets use canonical `+` order or NONE. Cohorts group refresh-related categories, other known categories and unknown/malformed items, without emitting their names or counts. Neither actual dates/times/offsets nor model/grid/location/category/value/body/URL/key are retained in the output. Unknown/forged strings become BINDING_UNKNOWN. A later issuance is relative to the request, not a claim that it is before the current clock or a valid refresh. Even a complete refresh-shaped summary cannot authorize acceptance changes; official binding evidence and a user interview precede that separate decision. Source: [official KMA APIhub forecast documentation](https://apihub.kma.go.kr/apiList.do?seqApi=10), including its linked 260623 guide. Those exact-SHA review/CI/deployment and single-reproduction steps are complete at f6ec59c; the failed outcome above remains non-GREEN.

Previous release readback on 2026-09-05: exact `c926198928ce4d04d89d01e2909b21c4d187a471` passed both independent correctness/data-equivalence and security reviews (`B0/H0/M0/L0`; each focused check `4 files / 111 PASS`). CI-only PR `#17` run `33952233286` and develop-push run `33952376883` both succeeded on the exact SHA. Initial and settled pre-promotion Deployment/Vercel check/status counts were all `0`. Only explicit Preview weather-timeline advanced to ACTIVE v10/JWT true; plan-route remains v11 and the other three functions v8, with JWT false only for kakao-oidc. After verifying unchanged remote base `92dc2de`, develop was fast-forwarded to the reviewed SHA; PR #17 records that same merge commit. GitHub Preview Deployment `6278408255` / status `17860805699` and Vercel `dpl_3NBUs8pm4UGxepNKjpEGxW1V71WA` are success/Ready at `https://motocast-6u76t6gbv-tocomboys-projects.vercel.app`, with the fixed develop alias attached. No DB, secret, other function, provider-policy, Production or paid-API change occurred.

One connected run using the unchanged public rest-comparison inputs on `c926198` is `PASS 0 / FAIL 1 / ERROR 0 / SKIP 0 / DESELECTED 0 / XFAIL 0 / SETUP_OR_IMPORT_FAILURE 0`. Route calculation/finalization, actual map and ordered legends passed; the weather status assertion failed, with bounded log `KMA_INVALID_RESPONSE BASE_TIME_MISMATCH` at `2026-09-05T07:24:57.617000`. The independently timed afterEach deleted the exact captured test trip without a cleanup failure. Collection save and share publish/revoke/reissue were NOT_RUN because weather blocked them; provider/search budget consumption remains charged. This label proves a matching valid base date and a valid string base time that differs from the requested time, not a numeric-representation failure. It does not expose the returned time, offset, category or model, and does not establish why the provider returned it. Official APIhub guide `단기예보조회서비스_API활용가이드_260623.docx` describes hourly :30 ultra bases, availability after :45, and ten-minute updates for temperature/humidity/wind; the local base selection follows that published request schedule. The exact update-to-response binding is unresolved. Compare that contract and bounded evidence next, without guessing offsets, relaxing validation, bypassing budgets or changing more test places. The following preparation/previous-release entries are historical and superseded by this current non-GREEN connected result.

Prepared base-binding refinement (not deployed): keep the existing strict binding predicate intact and classify only its rejected branch. Eight additional fixed labels distinguish base date/time type, format, value mismatch, and a finite integer representation equivalent to the requested valid string (including lost leading zeros in a time). The numeric-equivalence path still throws `KMA_INVALID_RESPONSE`; it does not coerce, retry, change the forecast base, or accept a provider item. The formatter now allows 22 reasons including historical BASE_BINDING; no returned/requested dates, times, values, offsets, grid, model, coordinates, body, URL or key are logged. Acceptance, first date-before-time rejection, all other validation, public responses, request policy, budget and persistence are unchanged. Writer: focused `4 files / 111 PASS`, full `57 files / 483 PASS`, lint/typecheck/Deno 5/build and Chromium `20 PASS / 2 connected SKIP`; failures and setup/import failures `0`. Existing identical-lockfile dependencies were reused. Fixed-SHA correctness/data-equivalence and security/operations delta reviews, exact-head CI and only explicit Preview weather-timeline deployment with JWT true remain required. The currently deployed state and last live failure are recorded below; do not treat any new label as observed before that reproduction.

Current release readback on 2026-09-05: exact `92dc2de3176c6915aba6481405b3bccd8b486dc1` passed independent correctness/data-equivalence and security/operations review (`B0/H0/M0/L0`), closing the raw-message logging MEDIUM. The unchanged UI test slice was independently approved at `37730cc`. CI-only PR `#16` run `33951168970` and develop-push run `33951341002` both succeeded on exact `92dc2de`; this closes the prior post-push UI test race. Initial and settled pre-promotion reads showed Deployment/Vercel checks/statuses `0`. Only explicit Preview weather-timeline advanced to ACTIVE v9 with JWT true; plan-route remains v11, search-places/save-collection/kakao-oidc v8, and only kakao-oidc has JWT false. The unchanged `0738f76` base was fast-forwarded to the reviewed SHA. Preview Deployment `6278227807`, status `17860360938`, and Vercel `dpl_CHR4pyGzKEA5ePxzjkTFbSxaiGZR` are success/Ready at `https://motocast-msssmh6se-tocomboys-projects.vercel.app`, with the fixed develop alias attached. No DB, secret, provider-policy, other function, Production or paid-API change occurred.

The same route-successful public rest-comparison input was tested once on `92dc2de`: `PASS 0 / FAIL 1 / SKIP 0 / SETUP_OR_IMPORT_FAILURE 0`. Actual route calculation/finalization/map and ordered legends passed; weather failed at `2026-09-05T07:01:53.878000` with `KMA_INVALID_RESPONSE BASE_BINDING`. The independently timed afterEach deleted the exact captured test trip; collection save and share publication were not reached, and no cleanup failure was reported. Provider/search budget use remains charged. BASE_BINDING means a returned base date/time fails exact request binding or date/time validation; it does not identify which field, its type, or actual value. Do not claim missing POP, a transport error, or a confirmed time offset. The next safe step is distinguishing base-field type/format/value mismatch without logging dates/times, locations, bodies, URLs or keys and without changing acceptance. This remains a failed connected gate. Preparation and older release statements below describe their historical phase and are superseded by this readback.

Prepared KMA diagnostic refinement (not deployed): retain the exact `KMA_INVALID_RESPONSE` message, rejection order/predicates, model/category requirements, provider request count, budget-before-provider, snapshots and public HTTP body/status. A typed validation error carries only one of fourteen closed reasons: JSON/object/item shape, base binding, category shape, forecast identity, value contract, grid binding, duplicate identity, or missing temperature/POP/WSD/SKY/PTY. Foreign or forged reasons become `UNKNOWN`; no model, item/point position or count, time, grid, coordinate, category value, body, message or URL is logged. Both terminal failure and stale-fallback logs replace arbitrary `Error.message` with an allowlisted weather code (plus bounded HTTP status or two-digit provider result code) and the closed reason. This addresses the security review's raw-message logging MEDIUM without weakening weather acceptance. Actual handler tests cover known/foreign/forged failures, stale fallback, budget denial before fetch, success and cache reuse. Their auth/route storage dependencies are mocked and do not prove hosted RLS. Initial focused run was `51 PASS / 33 FAIL` because assertions still expected plain Error objects; expectations now assert the new typed error and exact reason, preserving the same failure message. Final writer evidence: focused `86 PASS`, full `57 files / 458 PASS`, lint/typecheck/Deno 5/build and Chromium `20 PASS / 2 connected SKIP`; setup/import failures `0`, dependency installation reused with identical lockfile. Fixed-SHA independent security/correctness/operations review and exact-head CI are pending. Deploy only explicit Preview `weather-timeline` with JWT true after those gates, leaving plan-route v11 and other functions unchanged. Reproduce using the already route-successful public rest comparison; do not diagnose by changing more locations or bypassing budget accounting. Production remains out of scope.

Chunk-context release on 2026-09-05: exact `0738f76a1700776525e8d33d9e48644a5fe6d0c8` passed both independent review axes (`B0/H0/M0/L0`) and CI-only PR `#15` run `33949705166`. Initial and settled pre-promotion reads found Deployment/Vercel checks/statuses `0`. Only explicit Preview plan-route advanced to ACTIVE v11, JWT true; other functions remain v8 and kakao-oidc alone has JWT false. The unchanged `52f9cb5` develop base was fast-forwarded to the reviewed SHA. Preview Deployment `6277981877`, status `17859748097`, and Vercel `dpl_E9sT9rdeSUdQKzxu8yi51FYrjWSS` are success/Ready at `https://motocast-mawj7i1gf-tocomboys-projects.vercel.app`, with the fixed develop alias attached. Post-push CI `33949908643` is **FAIL**, despite the same-head PR run passing: Chromium `19 PASS / 1 FAIL / 2 connected SKIP`, subsequent build `SKIP`. The read-only UI reviewer traced the 390px failure to a 44px button measured as `43.99993896484375` during the panel's 220ms transform transition, with no UI/CSS diff since `840a8eb`. A test-only follow-up waits for that transition to finish while retaining the exact 44px assertion; this is not yet deployed evidence.

The original-input connected test is `FAIL 1`: at `2026-09-05T06:29:57.694000`, code `RESULT_CODE_106` has context `FUTURE_P1_P3_REST`, identifying the second provider request from lunch through the mandatory waypoint to rest. It did not reach finalization and had no trip/collection/share cleanup obligation. Changing only the runtime public REST query then passed route calculation, finalization, actual-map display and ordered role legends, but the same full scenario is **FAIL 1**, not a complete pass: weather failed with `KMA_INVALID_RESPONSE` at `2026-09-05T06:32:26.069000`. Its afterEach deleted the exact captured test trip; collection save and share publication were not reached. Combined connected taxonomy is `PASS 0 / FAIL 2 / SKIP 0 / SETUP_OR_IMPORT_FAILURE 0`; search/provider budget usage is retained. The first route failure is input-specific evidence, not independent proof of a real road incident. Do not vary more places or weaken route/weather guards. Identify which KMA validation branch rejected the response with bounded diagnostics before any parser change. Production, DB, secrets, provider policy and paid APIs remain unchanged. Earlier release/version assertions below are historical.

Local UI test-race correction validation: the same production panel transition is awaited through `getAnimations().finished` before measuring button geometry. No CSS, epsilon, rounding, 44px assertion reduction, retry or skip was introduced. Repeated focused browser checks pass `30/30` (320/390/820, ten each); full Chromium is `20 PASS / 2 connected SKIP`; Vitest `56 files / 412 PASS`, lint, typecheck, Deno five entrypoints and build pass. Setup/import failures `0`. The identical-lockfile dependency installation is reused. Fixed-SHA UI/operations delta review approved `37730cc` (`B0/H0/M0/L0`), resolving the stale Incomplete-list MEDIUM from `c4a8975`; hosted CI remains pending. This test/docs-only slice does not require an Edge or database deployment and cannot close the connected weather failure; the separately prepared KMA slice above has its own weather-function deployment boundary.

The deployed chunk-context formatter preserves result-code diagnostics and public responses while attaching only provider-request mode, zero-based overall occurrence start/end indices, and the chunk destination's closed role. It emits `CURRENT|FUTURE_P<from>_P<to>_<role>` with `0 <= from < to <= 29`, at most six transitions per chunk, and role `DESTINATION`, `WAYPOINT`, `LUNCH`, `DINNER`, or `REST`; absent/forged context becomes `UNKNOWN`. Position 0 is the overall origin. No IDs, names, coordinates, addresses, departure/arrival times, dwell or provider messages are logged. Only an already-rejected typed provider validation error is rethrown with context; budget refusal and other errors retain their original paths. Budget-before-provider, one call per chunk, no hidden retry, point order, safety options, geometry acceptance and persistence remain unchanged.

Result-code refinement release on 2026-09-05: exact `52f9cb5a14580af168f1ccf852302ef7eee114e3` passed final correctness/route-safety/data-equivalence and security/operations review (`B0/H0/M0/L0`), CI-only PR `#14` run `33948518098` and develop-push run `33948654549`. Repeated pre-promotion reads found Deployment/Vercel checks/statuses `0`; after only Preview plan-route advanced to ACTIVE v10 with JWT true, the unchanged `1dc01b1` base was fast-forwarded to the exact reviewed SHA. Other functions remain v8, with only kakao-oidc JWT false. Preview Deployment `6277767391`, status `17859203446`, and Vercel `dpl_HKuXGn2MfZkMNHqSFtQut6ouR6Ps` report success/Ready at `https://motocast-2tjidmab1-tocomboys-projects.vercel.app`, attached to the fixed develop alias. The closed categories distinguish SHAPE, eighteen documented literal codes and UNDOCUMENTED without changing rejection or public response. Two connected runs are `FAIL 2 / PASS 0 / SKIP 0 / SETUP_OR_IMPORT_FAILURE 0`: original inputs logged `RESULT_CODE_106` at `2026-09-05T06:02:18.235000`, and changing only the overall destination logged it again at `2026-09-05T06:05:08.078000`. Neither reached a finalization request, collection save or share publication; both cleanup hooks completed without trip/collection/share obligations, while provider/search budget use remains charged. In the [official table](https://developers.kakaomobility.com/guide/navi-api/reference), 106 concerns an incident around the request destination. Because the application splits provider requests at positive-dwell stops, this may be a lunch/rest occurrence rather than the overall destination. Which chunk failed remains unknown. Do not keep changing test places or weaken safety/geometry guards; obtain bounded chunk-context evidence first. No DB, provider-policy, secret, other function or Production change occurred. Earlier release readbacks below are historical, not current function-version assertions.

Release readback on 2026-09-05: exact SHA `1dc01b19d39d26277df6a6147e465f9763188c76` is now deployed after independent correctness/route-safety/data-integrity and security/operations approvals (`B0/H0/M0/L0`), CI-only PR `#13` run `33947339695`, and repeated zero-Deployment/Vercel pre-promotion checks. Only Preview `plan-route` advanced from v8 to ACTIVE v9, retaining `verify_jwt=true`; the other four functions remain v8. No migration or secret changed. The unchanged `840a8eb` develop base was fast-forwarded to that exact SHA; develop-push CI `33947488231` succeeded. GitHub Preview Deployment `6277573831`, status `17858701830`, and Vercel `dpl_4GEDqYB8HeqB7d9tS7poRsKdZTRt` report success/Ready at `https://motocast-38glsj8an-tocomboys-projects.vercel.app`, with the fixed develop alias attached. Authenticated read-only management passes `1/1`. The initial mutation attempt was rejected before execution (`NOT_RUN 1`). After the user explicitly approved one complete Preview run including test-owned creation, publication/revocation/reissue and cleanup, that single run executed and failed (`PASS 0 / FAIL 1 / SKIP 0 / SETUP_OR_IMPORT_FAILURE 0`; two 120-second finalization request/response timeout diagnostics belong to this one failed test). The Preview server log at `2026-09-05T05:40:10.549000` records only `ROUTE_RESPONSE_INVALID RESULT_CODE`. In the reviewed parser this means a non-integer/missing result code or an integer other than accepted success `0` and the separately classified no-route `1`; the exact value is not observed. This rejection precedes summary, geometry and aggregate validation for the rejected route, so those guards must not be relaxed on this evidence. No finalization request occurred; collection and share operations were never reached, and the completed afterEach found no trip/collection/share cleanup obligation. Provider/search budget use is not undone. This is an observed failure category, not a complete root-cause diagnosis or successful connected gate. The user subsequently expanded authorization to the Preview tests needed for diagnosis and correction; continue bounded provider use and exact test-owned cleanup without requesting each test again. This does not authorize Production changes or paid API activation. The previous `502` failures and full Preview gate remain unresolved; Production/main is still `d0134ed93d7e0d8aed1123c5d693c665bbe646e8`.

The route-response diagnostic follow-up preserves every acceptance check and tolerance. A typed internal error retains the exact `INVALID_ROUTE_PROVIDER_RESPONSE` message for the existing public HTTP status/code mapping, while the server log emits only the public failure code plus a closed validation category (such as section totals, road continuity, requested-point snapping, or JSON shape). It never includes numeric route data, a provider result message/body, a URL, a location, an error object, or a key. Unknown and forged diagnostic labels become `UNKNOWN`. The deployed `plan-route` handler has direct regression coverage for known, foreign, and forged error values. This is observability work, not proof of a route fix: after writer verification, independent correctness/route-safety and security review, and exact-head CI, deploy only the reviewed Preview `plan-route`, read its version/JWT setting, then perform one bounded real reproduction and inspect the fixed category. No migration, other Edge Function, provider-policy or Production change is part of this diagnostic rollout.

The browser continues to receive only the safe generic provider error or a stale snapshot. A successful KMA payload is accepted only when every item matches the requested base issue and grid, has a unique forecast-time/category identity, and gives finite category-bounded values for the temperature, precipitation probability, wind, sky, and precipitation-type fields used by the UI. Edge logs may record a KMA HTTP status or the documented short `resultCode` after constraining it to at most sixteen ASCII letters, digits, `_`, or `-`; all other values become `UNKNOWN`. Never log the `authKey`, request URL, response body, `resultMsg`, forecast payload, or browser authorization. A connected smoke must set a future Seoul departure before calculating the route so a past static form default cannot be misclassified as provider failure.

The bounded KMA diagnostic and live-test-time correction following `d187c490` was included in the completed `57cfa83` Preview release and its authenticated weather/collection/share cleanup scenario passed. Any subsequent UI-only follow-up must not redeploy a migration or Edge Function: after fixed-SHA approval, push only the exact reviewed SHA to a slash-free non-deploying `review-*` branch, prove zero GitHub Deployments and zero Vercel checks, pass exact-head `develop` PR CI, re-read the unchanged `origin/develop` base, then fast-forward and verify the exact Vercel Preview plus connected mobile/desktop behavior. Production remains untouched; the existing regions are retained under the confirmed `OPS-008` decision.

## Return estimate and legacy storage boundary

The planner accepts a ride date and departure time only. The browser exposes a Seoul-time minimum for guidance, while `plan-route` uses its trusted clock to reject a past departure before budget or provider work; an exact-boundary departure remains valid. Lunch and dinner are independently optional. It derives the recommended route's `returnAt` from validated Kakao section durations plus only selected meal and rest dwell, accepts a direct endpoint-only course and a return after Seoul midnight, and rejects a computed duration of 24 hours or more. The browser cannot supply a desired or hard return value that affects this decision.

The initial `trips` table still has non-null `desired_return_at` and `hard_return_at` columns. Until a separate data migration removes them, the trusted Edge Function supplies the Seoul departure-day end as an undisplayed compatibility value to the private persistence path. It is not a deadline, does not reject routes, and must never be exposed in current schemaVersion 3 share snapshots. Existing immutable schemaVersion 1 shares remain readable with their historical fields, and schemaVersion 2 remains readable without reintroducing removed return inputs.

## Complete-course collections and concise sharing

Each current collection version contains a verified origin, verified destination, and the complete ordered occurrence list. An occurrence keeps its stable ID, kind, dwell, stop role, and legacy pass-through marker; independently optional lunch and dinner, 0–5 rests, and repeated use of the same Kakao place must survive save, apply, cross-role reorder, and share preparation. An empty occurrence list is a valid direct course. Waypoint-only versions created in disposable Preview data remain stored but are not presented as complete reusable courses, and this rollout does not rewrite them.

`공유 준비` from a collection first replaces the planner input with that complete course. It must then calculate and persist a new safe route and its route-bound weather before opening the approval preview exactly once; a failed or stale calculation must not open or publish a share. Editing route input, applying a collection, or starting another calculation invalidates the prior preview request and approval session even if the saved trip ID is later reused. This session invalidation must not remount or reload the independent share-link history. Overlapping history reads commit only the latest-started response, so an older snapshot cannot hide a newer publish or restore an obsolete revoke state. If fresh weather expires without an input event, the share controls fail closed and their live status announces that a new route-bound forecast is required. The approval preview and public link use the same compact rendering: trip date and timing, one selected travel-route summary with ordered arrivals and dwell, the actual road map, and segment weather. Do not add raw coordinate lists or repeat unselected historical candidates. Preview smoke must verify that preparation never publishes automatically and that explicit publish, revoke, and reissue still operate on immutable snapshots.

## Supabase promotion order

The single-route browser and `plan-route` function are a coupled contract
change: the new browser omits `candidate`, the old function requires it, and
the new function rejects the old browser's candidate policy. Use a communicated
Preview maintenance window. Apply the database migration, deploy the reviewed
Edge Functions, then deploy the exact reviewed Vercel SHA; route calculation
stays closed until the migration head, deployed function version/JWT setting,
and Vercel SHA/Ready readbacks agree. Never describe a mixed-version interval
as available.

Rollback is coupled as well. Before applying this migration to Preview or
Production, prepare and independently review an exact rollback Web artifact
based on the previous application that can read immutable schemaVersion 3
shares while retaining its previous route-writing contract. Record its fixed
SHA and build evidence in this runbook. A rollout without that artifact is
forbidden: revoking existing test links is insufficient because the migrated
database continues to create schemaVersion 3 previews. During rollback, first
deploy that reader-compatible Web artifact, then roll back the matching Edge
Functions together; do not downgrade the database writer, rewrite an issued
snapshot, or claim sharing recovery until a new share preview and an existing
schemaVersion 3 link both read back successfully. Production remains blocked
by separate post-Preview configuration/promotion approval and this rollback gate.

1. After confirming the exact target and receiving approval, reset only the disposable local PostgreSQL 17 instance at `127.0.0.1:54322`; then apply all migrations from an empty database and run the explicit database tests. Never use this reset against either hosted project.
2. Obtain fixed-SHA independent data-integrity and security approval.
3. Create the reader-compatible rollback Web artifact described above from the previous deployed application, independently review it, and record its exact SHA plus schemaVersion 1/2/3 read tests. Do not apply the Preview migration before this artifact exists.

   Invalidated historical artifact: remote branch `rollback/schema-v3-reader-20260901`, exact SHA `9f4cb83d5ccf1e327b318dc843f2a32a7bf67518`, remains evidence only for schemaVersion 1/2/3 share reading and the earlier single-route migration. It is **not** a rollback artifact for `20260901140000_complete_course_collections.sql`: its legacy `save-collection` function calls the removed five-argument internal save RPC and its Web UI still permits waypoint-only collection writes. Deploying it with the ten-migration database would therefore expose a known-broken collection mutation path. Do not use it for this rollout.

   Superseded replacement artifact: remote slash-free branch `rollback-complete-course-20260902`, exact SHA `0e09827fbd5dc67faaf9952ca6bdc28d49bf3f77`, remains evidence for the closed collection writer and schemaVersion 1/2/3 parsing but is no longer the prepared rollback. Its shared-route reader uses exact place coordinates, so a schemaVersion 3 route-cache point that differs minutely from the DB-projected waypoint can be reported falsely as `선택 경로 미통과`.

   Superseded reader-corrected artifact: remote slash-free branch `rollback-complete-course-reader-20260902`, exact SHA `b80edabcf7f46fe7cc473d014bcfdec779aca734`, remains evidence for complete and legacy occurrence rendering but is no longer the prepared rollback. Its share consumer removes only the initial URL fragment; a second `/share#token` navigation in the same document can retain the new bearer and the previous rendered state.

   Prepared replacement artifact: remote slash-free branch `rollback-complete-course-reader-20260902`, exact SHA `1d1b07a51ae36e62d84bf5a0a9169fbf35b65c21`. It retains the superseded artifacts' visibly closed collection controls and reads, fixed HTTP 503 `save-collection` handler, legacy new-trip writer, immutable schemaVersion 1/2/3 parsers, complete schemaVersion 3 occurrence mapping, one-microdegree legacy coordinate tolerance, historical `winding` roles, and real `선택 경로 미통과` markers. Its share consumer also removes every same-document bearer fragment before resolution and prevents an older delayed response or JSON parse from overwriting the newest snapshot. Independent correctness, data-integrity, security, and operations reviews all report `BLOCKER 0 / HIGH 0 / MEDIUM 0 / LOW 0`. Writer evidence passes ESLint, TypeScript, full Vitest (`53 files / 299 tests`), all five Edge Function Deno checks, the 13-route production build, deterministic Chromium (`14 PASS / 2 SKIP`), and exact-range diff check. The regression holds the first successful response at JSON parsing, renders a distinct second valid snapshot, then proves the late first snapshot cannot replace it. The earlier backport terminology failure remains accounted as `1/11 FAIL` before the historical `와인딩` expectation was restored and the focused suite passed `11/11`. The remote ref read back the exact SHA; initial and 30-second settled GitHub readbacks both found zero Deployments, zero check-runs including Vercel, and zero Actions runs. This satisfies the prepared-artifact prerequisite for the current Preview rollout. During an actual rollback, deploy only this artifact's Web and Edge bundle to the already-migrated database and complete its live smoke before reopening collection actions.

   Historical deployment-filter evidence remains applicable only to branch naming: the slash-containing branch created Vercel Preview Deployment `6197366768`, while same-tree probe `rollback-deploy-filter-probe-20260901` at `a641d684e9f0c8ed96467936faad4780a11ecd63` produced zero Deployments and zero checks. Use only a slash-free `rollback-*` branch and verify zero deployment activation before treating a future rollback artifact as safely staged.
4. Push the exact reviewed release SHA only to a slash-free branch explicitly excluded by `vercel.json` (for example `review-single-route-<short-sha>`) and open a pull request into `develop` so hosted CI can run without deploying the incompatible Web first. Confirm the exact remote branch SHA, wait for webhook settlement, and require zero GitHub Deployments and zero Vercel checks for that SHA before continuing. If any deployment appears, stop before Preview mutation. The CI workflow must check out `github.event.pull_request.head.sha`, fail if its actual `HEAD` differs, and report that exact SHA in the run metadata; a synthetic PR merge ref is not release evidence. Run hosted CI on the open PR, but do not merge it.
5. Dry-run Preview migration application:

   ```bash
   npx --yes supabase@2.116.0 db push --project-ref lehjmbgfpoemqcwxowbx --include-all --skip-vault --dry-run
   ```

6. Apply to Preview only, then read back migration versions, RLS and function ACLs.
7. During the Preview maintenance window, deploy `search-places`, `plan-route`, `weather-timeline`, `save-collection`, and `kakao-oidc` from the reviewed fixed SHA. The first four retain JWT verification; only `kakao-oidc` is intentionally public with `verify_jwt=false` under `AUTH-004`. Read back each deployed Edge Function version and JWT setting. Re-read `origin/develop`; if it no longer equals the base SHA used by the reviewed PR, stop and create/review a new fixed SHA. Otherwise fast-forward `develop` directly to the exact reviewed release SHA, rather than creating a merge/squash/rebase SHA, so Vercel deploys the same commit that passed writer verification, independent review, and PR CI. Read back the exact Git ref, GitHub Deployment SHA, Vercel Ready state, and commit tree before reopening route calculation; then close or confirm GitHub's resulting state for the CI-only PR.
8. Register Preview-only Auth provider redirects and server secrets through the Supabase Dashboard or masked input. Never put values in command arguments or shell history. Preview name-only readback confirms all ten application secret names, including `KAKAO_LOGIN_CLIENT_SECRET` and `KAKAO_OIDC_STATE_SECRET`; values are never read or printed. The authorize request must use the HTTPS callback derived from `SUPABASE_URL`, not an internal Edge `request.url`.
9. Use disposable Preview identities to execute the complete Preview gate. The first Kakao identity has been registered as the sole Preview administrator; use a separate invited identity for rider-isolation checks.
10. After completing the Preview gate and obtaining separate Production configuration/promotion approval, retain the existing project under `OPS-008`, back up its pre-cutover database, and repeat the reviewed migration/function sequence for Production. Region confirmation does not authorize a reset or deployment.

Preview browser automation is fail-closed to `https://motocast-git-develop-tocomboys-projects.vercel.app` and Supabase project `lehjmbgfpoemqcwxowbx`. Run auth and authenticated Preview automation from WSL/Linux, then create the storage state through `npm run test:e2e:auth` in a dedicated owner-private directory outside the repository; the helper writes a `0600` state file and origin/project metadata. Native Windows fails before browser launch because this workflow cannot prove owner-only NTFS ACLs. `npm run test:e2e:preview` rejects missing metadata, symlinks, permissive files, another project, loopback, arbitrary HTTPS, and Production origins. Authenticated runs do not retain browser artifacts. The connected collection/share gate must prove saved-course restoration before network work, exact one-time route/finalize/weather/preview accounting, responsive owner surfaces at 320/390/820/1440, rest 0–5 controls, focused error feedback, and explicit publish. The database gate separately proves server rejection when weather is missing, stale, expires exactly now, expires after transaction start, or becomes stale after preview; the expiry checks use PostgreSQL's advancing `clock_timestamp()` rather than a transaction-start timestamp. Live mutation cleanup records its obligation before each request, captures exact nonsecret resource IDs from responses, and uses a separately timed `afterEach` hook to revoke only the test-owned active share, delete the exact collection, and call the ownership-enforcing `delete_owned_trip` boundary for the captured test trip; any unknown outcome or cleanup failure keeps the gate red.

Public share links use `/share#<token>`. The fragment is not sent in the initial HTTP request; the client copies it to component-local memory, synchronously removes it from browser history, and only then sends it in the JSON body of `POST /api/shares/resolve`. If history cleanup fails, resolution stops before any third-party map script can run. Do not reintroduce a dynamic `/share/<token>` page or `/api/shares/<token>` resolver because hosting request-path logs can then contain the bearer token.

Invitation links use `/invite#<token>` for the same reason. The fixed client removes the fragment from browser history and posts it to same-origin `application/json` `POST /api/invites/accept`, which sets the short-lived HttpOnly claim cookie. The endpoint rejects missing/mismatched Origin, cross-site Fetch Metadata, and non-JSON content before parsing or setting a cookie. Do not reintroduce `/invite/<token>` or log the accept request body.

## Edge Function secret names

Each Supabase project owns separate values for these names:

- `KAKAO_REST_API_KEY`
- `KAKAO_LOGIN_CLIENT_SECRET`
- `KAKAO_OIDC_STATE_SECRET`
- `PLACE_VERIFICATION_SECRET`
- `KMA_APIHUB_KEY`
- `ALLOWED_ORIGINS`
- `KAKAO_LOCAL_DAILY_LIMIT`
- `KAKAO_CURRENT_DAILY_LIMIT`
- `KAKAO_FUTURE_DAILY_LIMIT`
- `KMA_DAILY_LIMIT`

All limits must be positive integers. A missing, zero, negative, or exhausted limit fails closed. Failed provider attempts still consume budget under `COST-002`.

## Pre-ride and backup check

- Confirm both hosted projects are `ACTIVE_HEALTHY`; Preview inactivity is acceptable, Production unavailability before a ride is not.
- Confirm the Production deployment SHA matches the `main` merge SHA.
- Export a manual off-platform database backup before real rider data or schema promotion.
- Record the backup timestamp, source project ref, migration head, restore target, and restore readback without storing rider data in the public repository.
- Validate invite login, one minimal legal route, weather freshness, saved collection, publish/revoke, and the daily budget hard stop.
